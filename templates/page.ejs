<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <h1> <%= title.slice(0,-4) %> </h1>
        <div style="margin-bottom: 20px;white-space: pre;"><%= text %></div>
        <button>
            <a href="/editPage/<%=encodeURIComponent(title)%>">Edit</a>
        </button>
        <button id="delete">Delete</button>
        <button>
            <a href="/">Back</a>
        </button>
    </div>
    <script>
        let deleteButton = document.getElementById('delete')
        let blogTitle = "<%= title %>"

        deleteButton.onclick = function(event){
            fetch(`/${encodeURIComponent(blogTitle)}`, {
                method: 'DELETE'
            })

            window.location.href = 'http://localhost:5500/'
        }
    </script>
</body>
</html>